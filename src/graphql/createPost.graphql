mutation CreatePost($input: PostInput!) {
  createPost(input: $input) {
    id
    createdAt
    updatedAt
    title
    text
    type
    location
    price
    included
    availability
    points
    creatorId
  }
}
